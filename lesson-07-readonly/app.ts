const readOnlyArray: readonly number[] = [1, 2, 3]

const readOnlyTuple: readonly [string, number] = ['hello', 42]

let readOnlyObject: { readonly key: string; readonly value: number } = {
	key: 'example',
	value: 42
}
